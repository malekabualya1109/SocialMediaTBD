#for the moment it's based of a kaggle data set that is gonna try to mach different csv datas together 

#if that works, we can match the algorithm based on likes , interests etc.... to the posts

#pip install pandas
#pip install numpy
#pip install matplotlib
#pip install scikit-learn

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import sklearn
from sklearn.decomposition import TruncatedSVD
